---
title: "Recursion: it’s fonts all the way down"
layout: layouts/post.njk
teaser: "I’ve moved from Now Alt to Recursive, an open‑source variable font that unifies UI text and code with one flexible file."
image: "/blog/font-recursive-screengrab.jpg"
image_meta:
  text: "A demonstration of Recursive’s breadth."
  altext: "A sample of letter forms from Recursive."
date: 2025-09-04
url: /posts/20250904-switching-to-recursive.html
active_path: blog
tags:
  - note
  - posts
topics:
  - typography
  - performance
---

<div class="kh-note-box">
<span class="kh-note-box__label">Update (2026-07-30)</span>
The site has since moved on to <a href="https://www.ibm.com/plex/">IBM Plex</a>. Recursive was a fun experiment and I'm glad I ran it, but this is my personal site: it's my playground, and I was ready for something new.
</div>

{% markdown %}

It's nice to be [able to tinker with the site](/posts/20250831-site-overhaul-eleventy-v3/) more freely again -- in that spirit it's time to update the look, and while researching a more expressive font I found [Recursive](https://www.recursive.design/).

It's an excellent <span title="Variable fonts are single font files that behave like more than multiple font faces, letting you dynamically adjust width, slant, and other properties for more fun.">variable font</span> by [ArrowType (Stephen Nixon)](https://www.arrowtype.com/about), free and open‑source under the SIL Open Font License 1.1.

> Despite the challenge of production, a spectrum of expression within a single typeface has clear advantages for users. Some of the most compelling typographic layouts use multiple, complementary fonts. However, because different fonts usually have different metrics, such layouts can easily become a headache for visual design and technical implementation. Recursive, by contrast, provides multiple voices from a single, easy-to-use font file.

[Learn more about the Recursive and its creation here](https://www.recursive.design/process/).

Recursive is a lot of fun and I look forward to getting to know it better.

I chose to self‑host the WOFF2, it's a _not_ tiny 295 KB (roughly ~200 KB larger than my previous setup with Now Alt). I think it's worth it.

### Compare the two

You're already here, so here's a quick before and after of the near drop in change:

<article class="kh-bleed-out">
  <div class="kh-compare" style="--kh-compare-split: 50%;">
    <img class="kh-compare__img" src="/images/blog/font-change-now-alt.jpg" alt="The site set in Now Alt, before the change." loading="lazy" />
    <img class="kh-compare__img kh-compare__img--after" src="/images/blog/font-change-recursive.jpg" alt="The same page set in Recursive, after the change." loading="lazy" />
    <span class="kh-compare__label kh-compare__label--before" aria-hidden="true">Now Alt</span>
    <span class="kh-compare__label kh-compare__label--after" aria-hidden="true">Recursive</span>
    <span class="kh-compare__handle" aria-hidden="true"></span>
    <input class="kh-compare__range" type="range" min="0" max="100" value="50" step="1" aria-label="Reveal more of the Recursive version" />
  </div>
  <figcaption class="kh-text-body--3">Drag to compare: Now Alt (before) vs Recursive (after). Keyboard: focus the slider and use the arrow keys.</figcaption>
</article>

{# Kept deliberately small and local. This replaced image-compare-viewer from
   unpkg -- 53 KB to do what a range input and a clip-path do here, and the
   last third-party stylesheet on the site. The range input supplies pointer,
   touch and keyboard interaction; this only mirrors its value into the custom
   property the CSS reads. With no JavaScript the split stays at the inline
   50% and the figure reads as a static before/after. #}
<script>
  (function () {
    var figure = document.querySelector('.kh-compare');
    if (!figure) return;
    var range = figure.querySelector('.kh-compare__range');
    if (!range) return;
    var apply = function () {
      figure.style.setProperty('--kh-compare-split', range.value + '%');
    };
    range.addEventListener('input', apply);
    apply();
  })();
</script>

### A parting word for my old font

[Now Alt](https://usemodify.com/fonts/now-alt/)’s geometric warmth for headings was a pleasure, but I wanted something fresh and expressive. Thanks old friend.

{% endmarkdown %}

---
layout: layouts/base.njk
pageClass: posts
templateEngineOverride: njk
---

<section class="vf-content">
{% render '@ken-figure', {
  "text": image_meta.text,
  "alttext": image_meta.alttext,
  "imageUrl": "/images/crop-cinema" + image
} %}

<p><time datetime="{{ date }}" class="vf-summary__date">{{ date | dateDisplay }}</time> {% if topics %}&nbsp;&nbsp;<span class="vf-summary__author">Filed in: {{topics}}</span>{% endif %}</p>

<h1 class="vf-text-heading--1">{{title}}</h1>

{% if teaser -%}
<p class="vf-text-body--1">{{ teaser | safe }}</p>
{%- endif %}

{{ content | safe }}
</section>

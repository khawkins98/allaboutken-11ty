{
  "name": "feedback-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for allaboutken.com feedback buttons",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
